# Implementations

Each implementation lane must expose the same benchmark runner contract:

- setup
- run one question
- run full bank
- emit trace
- emit summary report

Implementation-specific optimizations are allowed only if they are documented and realistic for users of that approach.
