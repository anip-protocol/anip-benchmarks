# GTM Agent: raw-tools Lane

Implementation placeholder for the raw-tools benchmark lane.

This lane must run the same GTM Agent question bank and emit the common report schema from `runners/report-schema.json`.
