# Runners

Benchmark runners execute implementation lanes and write normalized reports.

Initial runner targets:

- Python: fastest path for GTM Agent parity with existing showcase scripts.
- TypeScript: useful for MCP/tooling baselines and broader JS agent ecosystem comparisons.
